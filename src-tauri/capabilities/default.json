{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and editor sub-windows",
  "windows": ["main", "editor-*"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
